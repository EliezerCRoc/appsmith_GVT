// Branding is included for every user on this CE build, so there is no upgrade
// prompt to show. Kept as a no-op component to preserve the import surface used
// by ee/utils/BusinessFeatures/brandingPageHelpers.
const UpgradeBanner = () => null;

export default UpgradeBanner;
